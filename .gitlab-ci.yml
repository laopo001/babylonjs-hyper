image: node:9

cache:
  paths:
  - node_modules/

stages:
  - deploy
  
打包:
  stage: deploy
  script:
    - echo 'start'
    - echo $(ls)
    - yarn install --registry=https://registry.npm.taobao.org
    - npm run build
    - npm run deploy
  only:
    - dev
  when: manual
  tags:
    - txy

